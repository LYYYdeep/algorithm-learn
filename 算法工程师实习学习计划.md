# 算法工程师实习学习计划

这是一份面向 **算法工程师实习 / NLP 算法实习 / 大模型算法实习 / 大模型训练工程实习** 的长期学习计划。目标不是短期刷完知识点，而是逐步建立：

- 机器学习基础
- 深度学习能力
- PyTorch 训练能力
- NLP / Transformer 理解
- Hugging Face 微调能力
- 大模型 LoRA / RAG 实战能力
- DeepSpeed / 分布式训练优化能力
- 可写进简历的项目经验

最终目标：

> 能独立完成机器学习、NLP、大模型微调、RAG、DeepSpeed 训练优化相关项目，并具备投递算法工程师实习岗位的能力。

---

## 目录

1. [整体路线图](#一整体路线图)
2. [阶段 1：基础能力](#二阶段-1基础能力)
3. [阶段 2：机器学习基础](#三阶段-2机器学习基础)
4. [阶段 3：深度学习与 PyTorch](#四阶段-3深度学习与-pytorch)
5. [阶段 4：NLP 与 Transformer](#五阶段-4nlp-与-transformer)
6. [阶段 5：Hugging Face 与预训练模型微调](#六阶段-5hugging-face-与预训练模型微调)
7. [阶段 6：大模型微调、LoRA、RAG](#七阶段-6大模型微调lorarag)
8. [阶段 7：DeepSpeed、分布式训练、训练优化](#八阶段-7deepspeed分布式训练训练优化)
9. [阶段 8：工程化能力](#九阶段-8工程化能力)
10. [推荐项目组合](#十推荐项目组合)
11. [最推荐的项目顺序](#十一最推荐的项目顺序)
12. [面试准备](#十二面试准备)
13. [算法题准备](#十三算法题准备)
14. [投递标准](#十四投递标准)
15. [简历关键词](#十五简历关键词)
16. [最终 GitHub 项目列表](#十六最终-github-项目列表)
17. [总结路线](#十七总结路线)

---

# 一、整体路线图

建议按以下 8 个阶段学习：

```text
阶段 1：Python、Linux、Git、数学基础
阶段 2：机器学习基础
阶段 3：深度学习与 PyTorch
阶段 4：NLP 与 Transformer
阶段 5：Hugging Face 与预训练模型微调
阶段 6：大模型微调、LoRA、RAG
阶段 7：DeepSpeed、分布式训练、训练优化
阶段 8：项目包装、简历、面试、投递
```

每个阶段都应该包括：

1. 理论知识
2. 代码练习
3. 小项目
4. 总结笔记
5. 可展示成果

注意：

> 不要只看视频或者文章。算法工程师实习最看重你能不能把模型跑起来、调起来、解释清楚。

---

# 二、阶段 1：基础能力

## 1. Python

需要非常熟悉 Python，至少达到能流畅写训练代码的程度。

重点学习：

- 基础语法
- 函数、类、装饰器
- list / dict / set / tuple
- 文件读写
- argparse
- logging
- pathlib
- tqdm
- json / yaml
- 虚拟环境管理

需要掌握的库：

```text
numpy
pandas
matplotlib
seaborn
scikit-learn
tqdm
jupyter
```

需要做到：

- 读取 CSV / JSON
- 清洗数据
- 统计数据分布
- 画图
- 写命令行训练脚本
- 保存实验结果

---

## 2. Linux / Shell

算法实习中很多训练是在服务器上跑的，所以 Linux 很重要。

重点学习：

```bash
cd
ls
pwd
mkdir
rm
cp
mv
grep
find
ps
kill
top
htop
du
df
chmod
ssh
scp
tmux
```

还要熟悉：

```bash
nvidia-smi
CUDA_VISIBLE_DEVICES
conda
pip
```

虽然当前使用的是 Mac，但以后跑云 GPU 一定会用到 Linux。

---

## 3. Git / GitHub

必须掌握：

```bash
git clone
git status
git add
git commit
git push
git pull
git branch
git checkout
```

所有项目都建议放到 GitHub。

一个好的项目仓库结构应该像这样：

```text
project-name/
├── README.md
├── requirements.txt
├── configs/
├── data/
├── scripts/
├── src/
├── notebooks/
└── outputs/
```

---

## 4. 数学基础

不用一开始学得特别深，但算法工程师必须懂这些。

### 线性代数

重点：

- 向量
- 矩阵
- 矩阵乘法
- 转置
- 点积
- 范数
- 特征值、特征向量
- PCA 基本思想

### 概率统计

重点：

- 均值、方差、标准差
- 条件概率
- 贝叶斯公式
- 最大似然估计
- 常见分布
- 置信区间基本概念

### 微积分 / 优化

重点：

- 导数
- 偏导
- 梯度
- 链式法则
- 梯度下降
- 学习率
- 凸优化基本概念

---

## 阶段 1 推荐项目

### 项目 1：数据分析项目

可以做：

- 豆瓣电影数据分析
- B 站视频数据分析
- 电商用户行为分析
- Kaggle Titanic 数据分析

项目目标：

- 数据读取
- 数据清洗
- 统计分析
- 可视化
- 写 README

这个项目的简历价值一般，但能帮助打基础。

---

# 三、阶段 2：机器学习基础

机器学习是算法工程师的地基。

## 1. 必学模型

需要理解这些模型的原理和使用方式：

```text
线性回归
逻辑回归
KNN
朴素贝叶斯
决策树
随机森林
GBDT
XGBoost
LightGBM
SVM
KMeans
PCA
```

重点不是背公式，而是要知道：

- 它解决什么问题？
- 输入输出是什么？
- 损失函数是什么？
- 怎么训练？
- 有哪些超参数？
- 什么时候容易过拟合？
- 怎么评估效果？

---

## 2. 必学概念

必须掌握：

```text
训练集 / 验证集 / 测试集
交叉验证
过拟合 / 欠拟合
正则化
特征工程
特征归一化
类别特征编码
缺失值处理
数据泄漏
样本不均衡
模型评估指标
超参数调优
```

---

## 3. 指标

分类任务：

```text
accuracy
precision
recall
F1
AUC
ROC
PR curve
confusion matrix
```

回归任务：

```text
MSE
RMSE
MAE
R2
```

排序 / 推荐任务：

```text
NDCG
MAP
MRR
Recall@K
HitRate@K
```

---

## 阶段 2 推荐项目

### 项目 2：用户流失预测

这是非常适合算法实习简历的项目。

内容：

- 用用户行为数据预测用户是否流失
- 做特征工程
- 对比 Logistic Regression、RandomForest、XGBoost、LightGBM
- 用 AUC / F1 评估
- 分析特征重要性

项目结构：

```text
churn-prediction/
├── README.md
├── data/
├── notebooks/
├── src/
│   ├── preprocess.py
│   ├── train.py
│   ├── evaluate.py
│   └── features.py
├── outputs/
└── requirements.txt
```

简历写法：

> 基于用户行为数据构建流失预测模型，完成缺失值处理、类别特征编码、特征工程和模型评估；对比 Logistic Regression、RandomForest、LightGBM 等模型，最终使用 LightGBM 将 AUC 提升至 XX。

---

### 项目 3：信用风险预测

内容：

- 金融风控二分类
- 处理类别不均衡
- 使用 LightGBM / XGBoost
- 输出 AUC、KS、F1

适合投：

- 风控算法
- 数据挖掘
- 机器学习实习

---

# 四、阶段 3：深度学习与 PyTorch

这个阶段决定你能不能进入深度学习、NLP、大模型方向。

## 1. 深度学习基础

重点概念：

```text
神经网络
前向传播
反向传播
损失函数
优化器
学习率
batch size
epoch
dropout
BatchNorm
LayerNorm
梯度消失
梯度爆炸
激活函数
权重初始化
```

常见激活函数：

```text
Sigmoid
Tanh
ReLU
GELU
SiLU
Softmax
```

常见优化器：

```text
SGD
Momentum
Adam
AdamW
```

---

## 2. PyTorch 必学内容

必须非常熟悉：

```python
torch.Tensor
torch.autograd
torch.nn.Module
torch.nn.Linear
torch.nn.Embedding
torch.utils.data.Dataset
torch.utils.data.DataLoader
torch.optim
loss.backward()
optimizer.step()
model.train()
model.eval()
torch.no_grad()
torch.save()
torch.load()
```

要能手写完整训练循环：

```python
for epoch in range(num_epochs):
    model.train()
    for batch in train_loader:
        inputs, labels = batch
        inputs = inputs.to(device)
        labels = labels.to(device)

        outputs = model(inputs)
        loss = criterion(outputs, labels)

        optimizer.zero_grad()
        loss.backward()
        optimizer.step()
```

---

## 3. Mac 的使用方式

Apple Silicon 可以使用 MPS：

```python
device = "mps" if torch.backends.mps.is_available() else "cpu"
```

适合训练：

- MLP
- CNN
- TextCNN
- BiLSTM
- 小 Transformer
- 小型 BERT 微调
- 小规模 LoRA demo

不适合：

- 大模型全参微调
- DeepSpeed 多卡训练
- CUDA/NCCL 实验

---

## 阶段 3 推荐项目

### 项目 4：CIFAR-10 图像分类

内容：

- 用 PyTorch 训练 CNN
- 再训练 ResNet
- 加入数据增强
- 对比不同优化器
- 画 loss / accuracy 曲线

学习价值：

- 熟悉 PyTorch
- 熟悉训练循环
- 熟悉模型评估

---

### 项目 5：中文文本分类

更推荐做这个，因为后面能接 NLP 和大模型。

内容：

- 数据集：中文情感分类、新闻分类、意图识别
- 模型：TextCNN、BiLSTM、Transformer Encoder
- 指标：Accuracy、F1
- 对比不同模型效果

项目结构：

```text
pytorch-text-classification/
├── README.md
├── data/
├── configs/
├── src/
│   ├── dataset.py
│   ├── model_textcnn.py
│   ├── model_bilstm.py
│   ├── model_transformer.py
│   ├── train.py
│   └── evaluate.py
└── requirements.txt
```

简历写法：

> 基于 PyTorch 实现中文文本分类系统，完成数据预处理、词表构建、Dataset/DataLoader 封装和训练评估流程；对比 TextCNN、BiLSTM、Transformer Encoder 在 Accuracy 和 F1 上的表现。

---

# 五、阶段 4：NLP 与 Transformer

如果要投大模型、NLP、LLM 算法实习，这个阶段非常重要。

## 1. NLP 基础

重点学：

```text
分词
词向量
Word2Vec
GloVe
FastText
Tokenization
BPE
WordPiece
SentencePiece
文本分类
序列标注
文本匹配
问答
摘要
语言模型
```

---

## 2. Transformer 必学

必须理解：

```text
Embedding
Position Encoding
Self-Attention
Multi-Head Attention
Feed Forward Network
Residual Connection
LayerNorm
Encoder
Decoder
Causal Mask
```

要能回答：

- Q、K、V 分别是什么？
- Attention 怎么计算？
- Multi-head attention 有什么意义？
- Transformer 为什么比 RNN 更适合并行？
- LayerNorm 和 BatchNorm 区别？
- BERT 和 GPT 区别？
- Encoder-only、Decoder-only、Encoder-Decoder 有什么区别？
- causal mask 是什么？

---

## 3. 经典模型

需要理解：

```text
BERT
RoBERTa
GPT
T5
LLaMA
Qwen
ChatGLM
```

重点不是每个都读源码，而是知道架构类别和任务方式。

---

## 阶段 4 推荐项目

### 项目 6：从零实现 Mini Transformer

这是一个非常加分的项目。

内容：

- 实现 Multi-Head Attention
- 实现 Position Embedding
- 实现 Transformer Block
- 训练一个小型语言模型
- 数据可以用 Tiny Shakespeare 或中文诗词

项目目标：

- 理解 Transformer 内部机制
- 能解释每一层作用
- 能画 loss 曲线
- 能生成文本

项目结构：

```text
mini-transformer-lm/
├── README.md
├── train.py
├── model.py
├── dataset.py
├── generate.py
└── configs/
```

简历写法：

> 使用 PyTorch 从零实现小型 Transformer 语言模型，包括 Multi-Head Attention、Position Embedding、Causal Mask 和 Transformer Block，并在文本数据集上完成训练与生成实验。

这个项目对面试很有帮助，因为面试官很容易追问 Transformer 细节。

---

# 六、阶段 5：Hugging Face 与预训练模型微调

这是从“会写模型”到“会用工业生态”的关键阶段。

## 1. 必学库

```text
transformers
datasets
tokenizers
evaluate
accelerate
peft
trl
```

---

## 2. Hugging Face 必学 API

要熟悉：

```python
AutoTokenizer
AutoModel
AutoModelForSequenceClassification
AutoModelForTokenClassification
AutoModelForCausalLM
Trainer
TrainingArguments
DataCollator
pipeline
```

例如：

```python
from transformers import AutoTokenizer, AutoModelForSequenceClassification

tokenizer = AutoTokenizer.from_pretrained("bert-base-chinese")
model = AutoModelForSequenceClassification.from_pretrained(
    "bert-base-chinese",
    num_labels=2,
)
```

---

## 3. 要掌握的任务

至少做过这些中的 2-3 个：

```text
文本分类
命名实体识别
文本匹配
问答
摘要
因果语言模型微调
指令微调
```

---

## 阶段 5 推荐项目

### 项目 7：BERT 中文文本分类

内容：

- 使用 `bert-base-chinese`
- 数据集可以是中文情感分类、新闻分类、意图识别
- 使用 Hugging Face Trainer
- 输出 Accuracy、F1
- 保存模型
- 写推理脚本

项目结构：

```text
bert-chinese-classification/
├── README.md
├── train.py
├── predict.py
├── evaluate.py
├── data/
├── configs/
└── requirements.txt
```

简历写法：

> 使用 Hugging Face Transformers 微调 BERT-base-chinese 完成中文文本分类任务，完成数据清洗、tokenization、Trainer 训练流程、模型评估与推理脚本开发，最终在测试集上取得 XX% F1。

---

### 项目 8：中文命名实体识别 NER

内容：

- 使用 BERT 做 token classification
- 数据集：人民日报 NER、CLUENER
- 评估实体级 F1

适合投 NLP 算法实习。

---

# 七、阶段 6：大模型微调、LoRA、RAG

这是现在算法实习非常有竞争力的方向。

## 1. 大模型基础

需要理解：

```text
LLM
预训练
SFT
Instruction Tuning
RLHF
DPO
Prompt Engineering
Context Length
Tokenizer
Chat Template
```

---

## 2. LoRA / PEFT

重点理解：

```text
全参微调
参数高效微调
LoRA
QLoRA
Adapter
Prefix Tuning
Prompt Tuning
```

LoRA 要能解释：

- 为什么省显存？
- rank 是什么？
- alpha 是什么？
- target_modules 是什么？
- 为什么常调 `q_proj`、`v_proj`？
- LoRA 权重怎么保存？
- LoRA 怎么 merge？

---

## 3. RAG

RAG 是实习非常适合做的项目，因为对硬件要求比训练大模型低，但工程和算法味道都比较足。

重点学习：

```text
Embedding
Chunking
Vector Database
FAISS
Chroma
Milvus
Retrieval
Rerank
Prompt Template
Citation
Hallucination
Evaluation
```

RAG 流程：

```text
文档加载
→ 文本切分
→ embedding
→ 向量库
→ 用户提问
→ 检索 Top-K
→ 拼接 Prompt
→ LLM 生成
→ 返回答案和引用
```

---

## 阶段 6 推荐项目

### 项目 9：Qwen 小模型 LoRA 指令微调

推荐模型：

```text
Qwen/Qwen2.5-0.5B-Instruct
Qwen/Qwen2.5-1.5B-Instruct
TinyLlama/TinyLlama-1.1B-Chat-v1.0
distilgpt2
```

Mac 可以先用 0.5B 或更小模型小数据调试，正式实验建议用云 GPU。

项目内容：

- 准备 SFT 数据集
- 使用 Transformers + PEFT
- 配置 LoRA
- 训练模型
- 保存 adapter
- 加载 adapter 推理
- 对比微调前后效果

项目结构：

```text
qwen-lora-sft/
├── README.md
├── data/
│   └── train.jsonl
├── configs/
│   └── lora_config.yaml
├── src/
│   ├── train_lora.py
│   ├── infer.py
│   └── merge_lora.py
├── outputs/
└── requirements.txt
```

简历写法：

> 基于 Transformers 和 PEFT 对 Qwen2.5-0.5B-Instruct 进行 LoRA 指令微调，构建 SFT 数据格式，完成训练、Adapter 保存、加载推理和效果对比；分析 LoRA rank、batch size、学习率对训练效果的影响。

---

### 项目 10：RAG 个人知识库问答系统

项目内容：

- 支持加载 PDF / Markdown / TXT
- 文档切分
- embedding 向量化
- FAISS 检索
- Top-K 召回
- prompt 拼接
- LLM 生成答案
- 返回引用来源

可以使用：

```text
sentence-transformers
FAISS
Transformers
FastAPI
Streamlit
```

项目结构：

```text
rag-knowledge-base/
├── README.md
├── app.py
├── src/
│   ├── loader.py
│   ├── splitter.py
│   ├── embedder.py
│   ├── retriever.py
│   ├── generator.py
│   └── evaluate.py
├── data/
├── vector_store/
└── requirements.txt
```

简历写法：

> 构建基于 RAG 的文档问答系统，使用 embedding 模型对文档切片向量化，基于 FAISS 实现语义检索，并结合大语言模型生成带引用的答案；优化 chunk size、Top-K 和 prompt 模板以提升回答准确率。

---

### 项目 11：RAG 检索优化实验

如果想更像算法项目，可以进一步做：

- 不同 chunk size 对比
- 不同 embedding 模型对比
- BM25 vs Dense Retrieval
- reranker 前后效果对比
- Top-K 影响实验
- 构建一批测试问题计算 Recall@K

这个比普通 RAG demo 更有含金量。

---

# 八、阶段 7：DeepSpeed、分布式训练、训练优化

这是偏大模型训练工程 / 算法 infra 的方向，非常加分。

## 1. 先学 PyTorch DDP

需要理解：

```text
rank
world size
local rank
process group
DistributedDataParallel
DistributedSampler
all_reduce
NCCL
```

会启动：

```bash
torchrun --nproc_per_node=2 train_ddp.py
```

---

## 2. 学 Hugging Face Accelerate

Accelerate 比 DeepSpeed 更容易入门。

重点学：

```bash
accelerate config
accelerate launch train.py
```

理解：

- mixed precision
- multi-GPU
- gradient accumulation
- distributed training

---

## 3. 学 DeepSpeed

重点掌握：

```text
DeepSpeedEngine
deepspeed.initialize
ZeRO-1
ZeRO-2
ZeRO-3
CPU Offload
NVMe Offload
gradient accumulation
activation checkpointing
mixed precision
checkpoint save/load
```

最核心的是 ZeRO：

| 技术 | 切分内容 |
|---|---|
| ZeRO-1 | optimizer states |
| ZeRO-2 | optimizer states + gradients |
| ZeRO-3 | optimizer states + gradients + parameters |

要能解释：

- DeepSpeed 解决什么问题？
- ZeRO 为什么省显存？
- ZeRO-2 和 ZeRO-3 区别？
- ZeRO-3 为什么通信开销更大？
- global batch size 怎么算？
- OOM 怎么处理？
- checkpoint 怎么保存和恢复？

---

## 4. Mac 怎么学 DeepSpeed

Mac 没有 NVIDIA CUDA，不适合真跑 DeepSpeed 多卡训练。

正确方式：

```text
本机：写代码、写配置、小规模 debug
云 GPU：正式跑 DeepSpeed 实验
```

推荐云 GPU：

```text
RTX 3090 24GB
RTX 4090 24GB
A10 24GB
A100 40GB
T4 16GB
```

如果只是学习，租 1-3 天就够做项目实验。

---

## 阶段 7 推荐项目

### 项目 12：DeepSpeed 显存优化实验

这是很适合写进简历的大模型训练项目。

项目内容：

- 训练一个小型 Transformer / GPT
- 对比普通 PyTorch、DDP、DeepSpeed
- 对比 ZeRO-1、ZeRO-2、ZeRO-3
- 记录显存占用
- 记录训练速度
- 分析 batch size、gradient accumulation、fp16/bf16 的影响

项目结构：

```text
deepspeed-memory-optimization/
├── README.md
├── train_pytorch.py
├── train_ddp.py
├── train_deepspeed.py
├── configs/
│   ├── ds_zero1.json
│   ├── ds_zero2.json
│   ├── ds_zero3.json
│   └── ds_zero2_offload.json
├── reports/
│   ├── memory_comparison.md
│   └── throughput_comparison.md
└── requirements.txt
```

README 里要有表格：

| 方法 | 显存占用 | tokens/s | batch size | 备注 |
|---|---:|---:|---:|---|
| PyTorch | XX GB | XX | XX | baseline |
| DDP | XX GB | XX | XX | 多卡 |
| ZeRO-1 | XX GB | XX | XX | 切 optimizer |
| ZeRO-2 | XX GB | XX | XX | 切 optimizer + grad |
| ZeRO-3 | XX GB | XX | XX | 切参数 |

简历写法：

> 基于 PyTorch 和 DeepSpeed 实现小型 Transformer 语言模型训练，对比 DDP 与 ZeRO-1/2/3 在显存占用和训练吞吐上的差异；结合 mixed precision、gradient accumulation 和 activation checkpointing 优化训练资源使用。

---

### 项目 13：Hugging Face + DeepSpeed 微调实验

内容：

- 使用 Hugging Face Trainer
- 接入 DeepSpeed 配置
- 微调 GPT2 / Qwen 0.5B / TinyLlama
- 对比不开 DeepSpeed 和开 ZeRO-2 的显存差异

项目重点：

- `TrainingArguments(deepspeed="ds_config.json")`
- ZeRO config
- checkpoint
- loss 曲线
- 显存记录

---

# 九、阶段 8：工程化能力

算法实习不只是模型，工程能力也很重要。

## 1. 必备工程能力

需要掌握：

```text
项目结构设计
配置文件管理
日志记录
实验记录
模型保存和加载
推理接口
简单 Web Demo
Docker 基础
FastAPI 基础
Streamlit 基础
```

---

## 2. 推荐工具

```text
wandb
tensorboard
hydra
omegaconf
pytest
black
ruff
docker
fastapi
streamlit
```

不需要全部精通，但至少要会：

- 用配置文件管理参数
- 用日志记录训练过程
- 用 README 说明怎么运行
- 用 requirements.txt 复现环境

---

# 十、推荐项目组合

不需要所有项目都做。建议从下面挑 4-5 个，形成简历项目矩阵。

---

## 方案 A：大模型算法实习方向

最推荐走这个方向。

项目组合：

```text
1. BERT 中文文本分类
2. Qwen LoRA 指令微调
3. RAG 个人知识库问答
4. DeepSpeed 显存优化实验
```

适合投：

```text
大模型算法实习
NLP 算法实习
LLM 应用算法实习
大模型训练实习
AI Agent / RAG 实习
```

---

## 方案 B：NLP 算法实习方向

项目组合：

```text
1. PyTorch 中文文本分类
2. BERT 中文文本分类
3. BERT NER
4. Qwen LoRA 微调
```

适合投：

```text
NLP 算法实习
文本分类算法实习
信息抽取实习
对话系统实习
```

---

## 方案 C：机器学习 / 数据挖掘方向

项目组合：

```text
1. 用户流失预测
2. 信用风险预测
3. 推荐系统召回/排序
4. LightGBM + 特征工程项目
```

适合投：

```text
机器学习算法实习
数据挖掘实习
风控算法实习
推荐算法实习
广告算法实习
```

---

## 方案 D：大模型训练工程方向

项目组合：

```text
1. Mini Transformer 从零实现
2. PyTorch DDP 训练
3. DeepSpeed ZeRO 显存优化实验
4. Hugging Face + DeepSpeed 微调
```

适合投：

```text
大模型训练工程实习
分布式训练实习
AI Infra 实习
模型训练平台实习
```

---

# 十一、最推荐的项目顺序

结合 DeepSpeed 和大模型方向，建议按这个顺序做：

## 第一个项目：用户流失预测 / LightGBM

目的：补机器学习基础。

会学到：

- 特征工程
- 模型评估
- sklearn
- LightGBM

---

## 第二个项目：PyTorch 中文文本分类

目的：练 PyTorch 基本功。

会学到：

- Dataset / DataLoader
- 训练循环
- 模型保存
- TextCNN / BiLSTM / Transformer Encoder

---

## 第三个项目：BERT 中文文本分类

目的：进入 Hugging Face 和预训练模型。

会学到：

- Tokenizer
- Transformer 模型加载
- Trainer
- 微调流程
- 推理脚本

---

## 第四个项目：Qwen LoRA 指令微调

目的：进入大模型实习方向。

会学到：

- SFT 数据格式
- LoRA
- PEFT
- 大模型推理
- Adapter 保存和加载

---

## 第五个项目：RAG 知识库问答

目的：做一个完整可展示的大模型应用算法项目。

会学到：

- embedding
- FAISS
- 检索
- prompt
- rerank
- 引用来源

---

## 第六个项目：DeepSpeed ZeRO 显存优化实验

目的：体现对大模型训练优化有理解。

会学到：

- DDP
- DeepSpeed
- ZeRO-1/2/3
- mixed precision
- gradient accumulation
- 显存分析

---

# 十二、面试准备

## 1. 机器学习高频

要能讲清楚：

```text
逻辑回归原理
交叉熵损失
AUC
F1
过拟合
正则化
决策树
随机森林
GBDT
XGBoost
LightGBM
样本不均衡处理
特征工程
```

---

## 2. 深度学习高频

```text
反向传播
梯度下降
Adam
AdamW
BatchNorm
LayerNorm
Dropout
学习率调度
梯度消失
梯度爆炸
过拟合处理
```

---

## 3. Transformer 高频

```text
Self-Attention
Q/K/V
Multi-Head Attention
Position Encoding
LayerNorm
Residual Connection
Causal Mask
BERT vs GPT
Encoder vs Decoder
```

---

## 4. 大模型高频

```text
SFT
LoRA
QLoRA
RAG
Embedding
Rerank
Prompt
Hallucination
Tokenizer
Context Length
DPO
RLHF 基本概念
```

---

## 5. DeepSpeed 高频

```text
DDP
ZeRO-1/2/3
gradient accumulation
mixed precision
FP16 vs BF16
CPU offload
activation checkpointing
NCCL
global batch size
OOM 排查
```

---

# 十三、算法题准备

算法题要刷，但不需要像纯后端那样刷得特别猛。

算法工程师实习常见考：

```text
数组
字符串
哈希表
双指针
二分
栈
队列
链表
树
DFS/BFS
动态规划基础
TopK
排序
```

目标：

- LeetCode 简单题稳定解决
- 中等题能做常见题型
- 总量 100-150 题左右比较够用

重点题型：

```text
两数之和
最长无重复子串
三数之和
二分查找
合并区间
Top K Frequent
LRU Cache
反转链表
层序遍历
最大子数组和
最长递增子序列
编辑距离
```

---

# 十四、投递标准

达到下面标准就可以开始投。

## 代码能力

能：

- 写 PyTorch 训练脚本
- 写 Hugging Face 微调脚本
- 写推理脚本
- 保存和加载模型
- 处理数据集
- 记录实验结果
- debug shape mismatch、OOM、loss 不下降

---

## 理论能力

能讲清：

- 机器学习经典模型
- 深度学习训练过程
- Transformer 结构
- BERT / GPT 区别
- LoRA 原理
- RAG 流程
- DeepSpeed ZeRO 原理

---

## 项目能力

至少有 3 个项目：

```text
1 个机器学习项目
1 个 NLP / BERT 项目
1 个大模型 / RAG / DeepSpeed 项目
```

最好有 4-5 个项目，其中 2 个重点项目写进简历。

---

# 十五、简历关键词

未来简历可以围绕这些关键词构建：

```text
Python
PyTorch
Scikit-learn
LightGBM
Transformers
Hugging Face
BERT
Qwen
LoRA
PEFT
RAG
FAISS
DeepSpeed
ZeRO
DDP
LLM Fine-tuning
NLP
Transformer
```

---

# 十六、最终 GitHub 项目列表

可以最终做成这样的 GitHub：

```text
github.com/yourname/ml-churn-prediction
github.com/yourname/pytorch-text-classification
github.com/yourname/bert-chinese-classification
github.com/yourname/qwen-lora-sft
github.com/yourname/rag-knowledge-base
github.com/yourname/deepspeed-memory-optimization
```

每个项目都要有：

```text
README.md
requirements.txt
训练命令
推理命令
实验结果
模型说明
数据说明
遇到的问题
改进方向
```

README 一定要认真写。很多实习筛简历时，项目描述和 GitHub README 很关键。

---

# 十七、总结路线

整体学习顺序：

```text
Python / 数学 / Linux / Git
→ 机器学习和 LightGBM
→ PyTorch 深度学习
→ NLP 和 Transformer
→ Hugging Face 微调
→ LoRA / RAG / 大模型应用
→ DeepSpeed / DDP / ZeRO
→ 项目包装 / 面试 / 投递
```

如果只选最核心的项目，建议做这 4 个：

```text
1. LightGBM 用户流失预测
2. BERT 中文文本分类
3. Qwen LoRA 指令微调
4. DeepSpeed ZeRO 显存优化实验
```

这 4 个项目做扎实，基本能覆盖：

- 传统机器学习
- NLP
- 大模型微调
- 大模型训练优化

对于投算法工程师实习，尤其是 NLP / 大模型方向，会比较有竞争力。
